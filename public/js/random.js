//Math.floor(Math.random() * 30) +1;

const randomTrip= Math.floor(Math.random() * 30) +1;

function userReturn (){

    //loop over location data for trip_id.

}

function getRndInteger(min, max) {
    return Math.floor(Math.random() * (max - min + 1) ) + min;
  }